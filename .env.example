# Copy this file to .env.local for local development.
# In production (Vercel), set these as Project Environment Variables instead.
#
# HTTP Basic Auth gate for the whole app (see src/middleware.ts).
# This is a single shared password to keep strangers off the public URL —
# not real per-user accounts.
APP_BASIC_AUTH_USER=
APP_BASIC_AUTH_PASSWORD=
